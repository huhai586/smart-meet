const isRestoreDataValid = (data: string) => {    return new Promise((resolve, reject) => {        try {            const jsons = JSON.parse(data);            if (!Array.isArray(jsons)) {                reject('the file is not json valid');            } else {                const isDataValided = jsons.every((item) => {                    return item.hasOwnProperty('activeSpeaker') && item.hasOwnProperty('session') && item.hasOwnProperty('talkContent') && item.hasOwnProperty('timestamp');                });                if (isDataValided) {                    resolve(jsons)                } else {                    reject('the file is not json valid');                }            }        } catch (e) {            reject('the file is not json valid');        }    });}export default isRestoreDataValid;